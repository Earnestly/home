#!/bin/zsh


read -r font < ${XDG_CONFIG_HOME:-$HOME/.config}/dmenu/font

opts=(
    -fn $font -p 'Run'
    -nf '#a7a7a7' -nb '#1e1e1e'
    -sf '#9b859d' -sb '#262626'
)

print -lo ${(k)commands} | dmenu $opts $@ | exec /bin/dash
