#!/bin/bash

# A simple wrapper around livestreamer

opts=(
    -p "mpv --profile=live"
    --ringbuffer-size 800000
    --default-stream medium,high,best,source,mobile
    "$1"
)

exec livestreamer "${opts[@]}"
