#!/bin/bash

# A simple wrapper around livestreamer

lopts=(
    -p "mpv --profile=live"
    --stream-sorting-excludes=1080p+,1080p
    --ringbuffer-size 800000
    "$1"
    "${2:-best}"
)

livestreamer -Q "${lopts[@]}"
