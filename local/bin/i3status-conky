#!/bin/bash

printf "%s%s%s" '{"version":1}' '[' '[],'

case $HOSTNAME in
    teapot) exec conky -c "$XDG_CONFIG_HOME"/conky/conkyrc.teapot ;;
    engine) exec conky -c "$XDG_CONFIG_HOME"/conky/conkyrc.engine ;;
esac
